# Local Next.js dev (npm run dev)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api

# Production URL lives in wrangler.jsonc → vars (and Cloudflare Build env — same value)
# NEXT_PUBLIC_API_URL=https://task-tribe-backend.onrender.com/api
